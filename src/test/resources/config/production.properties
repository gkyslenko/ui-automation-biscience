# Production environment configuration
# WARNING: Never commit real production credentials.
# Set auth.username and auth.password via CI secrets / environment variables.
base.url=https://ui.adcint.com/
base.username=${prod.username}

browser=chrome
headless=true
explicit.wait=20

screenshot.dir=test-output/screenshots
